/// Parses the stream of characters and produces a SerializationTree.
/// Parsing discards all the details introduced in the presentation process, reporting only the serialization tree.
/// Parsing can fail due to ill-formed input.
pub(crate) struct Parser {
}
